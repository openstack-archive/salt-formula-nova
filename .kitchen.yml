---
driver:
  name: vagrant
  vm_hostname: nova.ci.local
  use_sudo: false
  customize:
    memory: 512


provisioner:
  name: salt_solo
  salt_install: bootstrap
  salt_bootstrap_url: https://bootstrap.saltstack.com
  salt_version: latest
  formula: nova
  log_level: info
  state_top:
    base:
      "*":
        - nova
  pillars:
    top.sls:
      base:
        "*":
          - nova
  grains:
    noservices: False


verifier:
  name: inspec
  sudo: true


platforms:
  - name: ubuntu-14.04
  - name: ubuntu-16.04
  #- name: centos-7.1


suites:

  - name: compute-cluster
    provisioner:
      pillars-from-files:
        nova.sls: tests/pillar/compute_cluster.sls

  - name: compute-single
    provisioner:
      pillars-from-files:
        nova.sls: tests/pillar/compute_single.sls

  - name: control-cluster
    provisioner:
      pillars-from-files:
        nova.sls: tests/pillar/control_cluster.sls

  - name: control-single
    provisioner:
      pillars-from-files:
        nova.sls: tests/pillar/control_single.sls

# vim: ft=yaml sw=2 ts=2 sts=2 tw=125
